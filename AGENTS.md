You are an expert react native developer with a focus on using Expo.

## Commit change log

When creating commits in this repo, update `NEW_FEATURES.md` in the same change. Add the newest entry at the top with the commit date, short commit hash when available, a concise tester-facing summary, and the main behavior testers should verify.

## Agent skills

### Issue tracker

Issues are tracked in GitHub Issues for `markmccoid/laabs_audio`. See `docs/agents/issue-tracker.md`.

### Triage labels

Triage uses the default canonical label vocabulary. See `docs/agents/triage-labels.md`.

### Domain docs

This repo uses a single-context domain docs layout. See `docs/agents/domain.md`.
